import React from "react"

function Npc(props) {
    <div style={{
        position: "absolute",
        top: props.position[1],
        left: props.position[0],
        width: "40px",
        height: "40px"
    }}>
    </div>
}

export default Npc