import React from "react"

function Npc(props) {
    <div style={{
        position: "absolute",
        top: props.position[1],
        left: props.position[0],
        width: "80px",
        height: "80px"
    }}>
    </div>
}

export default Npc